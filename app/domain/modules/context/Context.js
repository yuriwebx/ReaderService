define([
   
],
function () {
   'use strict';

   var context = {};
   /* --- api --- */
   context.parameters   = {};
   context.serverUrl    = '';
   context.applicationUrl  = ''; // web-version url

   return context;

   /* --- impl --- */
});
